spring:
  application:
    name: gateway-service
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: 192.168.66.3 #nacos地址
      config:
        file-extension: yaml
        shared-configs:
          - data-id: shared-log.yaml